# DataBoard Analytics - Аналитический дашборд

Полнофункциональная система для анализа данных с возможностью подключения к PostgreSQL, очистки данных и визуализации метрик.

## 🚀 Быстрый старт

### Предварительные требования
- Node.js 18+ 
- npm/yarn
- PostgreSQL (локальный или удаленный)

### Установка и запуск

1. **Клонирование и установка зависимостей:**
```bash
git clone <repository-url>
cd databoard-analytics
npm install
cd server && npm install && cd ..
```

2. **Настройка переменных окружения:**
```bash
# Скопируйте пример конфигурации
cp .env.example .env
cp server/.env.example server/.env

# Отредактируйте .env файлы с вашими данными БД
```

3. **Запуск приложения:**
```bash
# Запуск полного стека (фронтенд + бэкенд)
npm run dev:full

# Или запуск отдельно:
npm run dev          # только фронтенд
npm run server:dev   # только бэкенд
```

4. **Доступ к приложению:**
- Фронтенд: http://localhost:5173
- API: http://localhost:3001/api
- Health check: http://localhost:3001/health

## 📊 Сквозной прогон: БД → Очистка → Дашборд

### Шаг 1: Подключение к базе данных

**Автоматическое подключение:**
1. Система автоматически пытается прямое подключение к PostgreSQL
2. При неудаче автоматически создает SSH туннель
3. Логи подключения отображаются в консоли

**Проверка подключения:**
```bash
curl http://localhost:3001/api/database/status
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "data": {
    "isConnected": true
  }
}
```

### Шаг 2: Очистка и подготовка данных

**Автоматические миграции:**
- Система автоматически запускает миграции при старте
- Создаются индексы для оптимизации запросов
- Настраиваются триггеры для аудита

**Ручная очистка данных:**
```bash
# Запуск скриптов очистки
python scripts/data_cleaning.py

# Результат: отчет с количеством обработанных записей
```

**Проверка ка��ества данных:**
```bash
curl http://localhost:3001/api/database/stats
```

### Шаг 3: Загрузка дашборда

**Автоматическая загрузка:**
1. Откройте http://localhost:5173
2. Дашборд автоматически загружает данные
3. Обновление данных каждые 30 секунд

**Доступные разделы:**
- **Dashboard** - основные метрики
- **Analytics** - углубленная аналитика  
- **Database** - управление БД
- **Admin** - административные функции

## 📁 Структура проекта

```
databoard-analytics/
├── src/                    # Фронтенд (Vue.js)
│   ├── components/        # Компоненты UI
│   ├── views/            # Страницы приложения
│   ├── services/         # API сервисы
│   └── router/           # Маршрутизация
├── server/                # Бэкенд (Express.js)
│   ├── controllers/      # Контроллеры API
│   ├── routes/           # Маршруты API
│   ├── config/           # Конфигурация БД
│   └── app.js           # Основной ф��йл сервера
├── sql/                  # SQL запросы
├── scripts/              # Скрипты очистки данных
├── docs/                 # Документация
└── .env                  # Переменные окружения
```

## 🔧 API Endpoints

### Основные эндпоинты:
- `GET /api/database/stats` - Статистика БД
- `GET /api/database/tables` - Список таблиц
- `GET /api/database/status` - Статус подключения
- `POST /api/database/query` - Выполнение SQL запросов

### Аналитические эндпоинты:
- `GET /api/database/performance` - Метрики производительности
- `GET /api/database/monitoring/realtime` - Мониторинг в реальном времени
- `GET /api/database/analytics/weekly` - Недельная активность

## 🛠 Разработка

### Команды разработки:
```bash
npm run dev              # Запуск фронтенда
npm run dev:full         # Запуск фронтенда + бэкенда
npm run build            # Сборка для продакшена
npm run test:unit        # Запуск тестов
npm run lint             # Проверка кода
npm run format           # Форматирование кода
```

### Структура компонентов:
```bash
src/components/
├── DatabaseStatsCards.vue     # Карточки статистики
├── QueryActivityWidget.vue    # Виджет активности запросов
├── DatabasePerformanceWidget.vue # Производительность БД
└── DashboardSidebar.vue       # Боковая панель
```

## 📋 Мониторинг и логирование

### Логи системы:
- **Подключение к БД:** 🔌 статусы подключения
- **SQL запросы:** 📊 время выполнения и запросы
- **API вызовы:** 📡 HTTP запросы и ответы
- **Ошибки:** ❌ детальная информация об ошибках

### Кэширование:
- Статистика БД: кэш 30 сек
- Список таблиц: кэш 60 сек
- Схемы таблиц: кэш 5 мин
- Аналитика: кэш 5 мин

## 🚨 Устранение неполадок

### Проблемы с подключением к БД:
1. Проверьте настройки в `.env`
2. Убедитесь что PostgreSQL доступен
3. Проверьте логи: `npm run server:dev`

### Ошибки API:
1. Проверьте статус бэкенда: http://localhost:3001/health
2. Очистите кэш: в DevTools Application → Storage
3. Перезапустите сервер: `npm run dev:full`

### Производительность:
1. Проверьте индексы в БД
2. Мониторинг соединений: `/api/database/performance`
3. Настройте размер пула соединений в `.env`

## 🔒 Безопасность

- Rate limiting: 100 запросов/15 мин
- CORS настроен для локальной разработки
- SQL инъекции защищены параметризованными запросами
- Логирование всех SQL операций

## 📊 Метрики и KPI

Система отслеживает следующие ключевые показатели:
- Общее количество таблиц
- Количество записей в БД
- Размер базы данных
- Активные подключения
- Производительность запросов
- Недельная активность

## 🎯 Следующие шаги

1. Настройте продакшен окружение
2. Добавьте аутен��ификацию пользователей
3. Расширьте аналитические возможности
4. Настройте автоматические бэкапы
5. Добавьте уведомления и алерты
